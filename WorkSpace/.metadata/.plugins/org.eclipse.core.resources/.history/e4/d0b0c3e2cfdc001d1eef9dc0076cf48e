package practice;

public class practice05 {
	//메인에서 배열을 메소드에게 넘김.
	//메소드는 배열의 누적합을 구해서 그 결과를 다시 사용자에게 줌
	int sum1(int[]arr) {
		int sum =0 ;
		for (int i = 0; i < arr.length; i++) {
			sum += arr[i];
		}
		return sum;
	}

}
